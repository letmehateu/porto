# Router

Instantiates a server framework-agnostic router for Porto.

<center>
  <img src="/router.svg" alt="Router" />
</center>

## Usage

```ts twoslash
// @noErrors
import { Router, Route } from 'porto/server'

const router = Router()
  .route('/merchant', Route.merchant({/* ... */})
  // .route('/auth', Route.auth({/* ... */}) 🚧
  // .route('/rpc', Route.relay({/* ... */}) 🚧
```

## Routes

| Route | Description |
| ----- | --------- |
| [`Route.merchant`](/sdk/api/route/merchant) | Route for Merchant RPC (call sponsoring, subscriptions, etc). |
| `Route.auth` 🚧 | Route for a user authentication service. |
| `Route.relay` 🚧 | Route for self-hosted Relay RPC. |

## Frameworks

### Cloudflare Workers

```ts
import { env } from 'cloudflare:workers'
import { Router, Route } from 'porto/server'

export default Router({ basePath: '/api' })
  .route('/merchant', Route.merchant({ /* ... */ })
  // .route('/auth', Route.auth({/* ... */}) 🚧
  // .route('/rpc', Route.relay({/* ... */}) 🚧
) satisfies ExportedHandler<Env>
```

### Next.js

```ts [app/api/[[...routes]]/route.ts]
import { Router, Route } from 'porto/server'

const router = Router({ basePath: '/api' })
  .route('/merchant', Route.merchant({ /* ... */ })
  // .route('/auth', Route.auth({/* ... */}) 🚧
  // .route('/rpc', Route.relay({/* ... */}) 🚧
)

export const GET = router.fetch
export const OPTIONS = router.fetch
export const POST = router.fetch
```

### Hono

```ts
import { Hono } from 'hono'
import { Router, Route } from 'porto/server'

const app = new Hono()

const router = Router()
  .route('/merchant', Route.merchant({ /* ... */ })
  // .route('/auth', Route.auth({/* ... */}) 🚧
  // .route('/rpc', Route.relay({/* ... */}) 🚧
)


app.route('/api', router.hono)

export default app
```

### Bun

```ts
import { Router, Route } from 'porto/server'

const router = Router({ basePath: '/api' })
  .route('/merchant', Route.merchant({ /* ... */ })
  // .route('/auth', Route.auth({/* ... */}) 🚧
  // .route('/rpc', Route.relay({/* ... */}) 🚧
)

Bun.serve(router)
```

### Deno

```ts
import { Router, Route } from 'porto/server'

const router = Router({ basePath: '/api' })
  .route('/merchant', Route.merchant({ /* ... */ })
  // .route('/auth', Route.auth({/* ... */}) 🚧
  // .route('/rpc', Route.relay({/* ... */}) 🚧
)

Deno.serve(router.fetch)
```

### Express

```ts
import express from 'express'
import { Router, Route } from 'porto/server'

const app = express()

const router = Router()
  .route('/merchant', Route.merchant({ /* ... */ })
  // .route('/auth', Route.auth({/* ... */}) 🚧
  // .route('/rpc', Route.relay({/* ... */}) 🚧
)

app.use('/api', router.listener)

app.listen(3000)
```

### Other Frameworks

If you would like to see an example of your framework here, please [open a discussion](https://github.com/ithacaxyz/porto/discussions/new) and we'll add it.
